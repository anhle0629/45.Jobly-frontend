import React from 'react'

//props: username, name first and las, email passing this down as context so i need a context folder
// props: updateing the user profile

function Profile ()  {

}

export default Profile
